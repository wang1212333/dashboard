import { historyIcons } from './history-icons.js'

export function historyManagementStyles(): string {
  return `.sidebar .sidebar-history{display:flex;flex:1;flex-direction:column;min-height:0;overflow:visible;margin:20px 12px 12px;padding:16px 0 0;border-top:1px solid #e5e5e5}.history-heading{display:flex;align-items:center;justify-content:space-between;padding:0 10px 1px;font-size:12px;font-weight:500;color:#8A8A8A;line-height:18px;flex-shrink:0}.history-heading button,.history-more,.history-rename button{display:grid;place-items:center;flex:0 0 30px;width:30px;height:30px;padding:6px;background:transparent;border:0;border-radius:6px;color:#555}.history-heading svg,.history-more svg,.history-rename svg,.history-footer svg,.history-group svg,.history-menu svg{width:18px;height:18px;flex-shrink:0}.history-scroll{overflow:auto;min-height:0;flex:1;margin-right:-9px;padding-right:9px;scrollbar-width:thin;scrollbar-color:#d6d6d6 transparent}.history-list{display:block}.history-group{display:flex;align-items:center;gap:6px;padding:12px 10px 7px;font-size:12px;color:#777}.history-group button{display:flex;align-items:center;gap:6px;border:0;border-radius:6px;background:transparent;color:inherit;padding:3px 6px;margin-left:-6px;min-height:24px;font:inherit;cursor:pointer}.history-group button:hover,.history-group button:focus{background:#ECECED;outline:none}.history-group button:active{background:#E2E2E4}.history-group button:focus-visible{outline:2px solid #999;outline-offset:2px}.history-row{display:flex;align-items:center;min-height:36px;padding-right:6px;border-radius:8px;margin:0 0 4px;position:relative}.history-row.is-pinned:has(+.history-row:not(.is-pinned))::after{content:"";position:absolute;left:10px;right:10px;bottom:-2px;height:1px;background:#E5E5E5;pointer-events:none}.history-row:hover{background:#f2f2f2}.history-row.is-active,.history-row.menu-open{background:#ececed}.history-row .history-item{flex:1;min-width:0;padding:8px 10px;gap:0;background:transparent}.history-row .history-item strong{font-size:13px;line-height:19px;font-weight:400}.history-row .history-item span{font-size:11px;line-height:15px;font-weight:400;color:#8A8A8A}.history-more{opacity:0;flex-shrink:0}.history-row:hover .history-more,.history-row:focus-within .history-more,.history-row.is-active .history-more,.history-row.menu-open .history-more{opacity:1}.history-more:hover,.history-heading button:hover{background:#e2e2e4}.history-footer{display:flex;align-items:center;gap:9px;flex-shrink:0;width:100%;border:0;border-top:1px solid #e5e5e5;padding:14px 8px 3px;margin-top:12px;background:transparent;color:#555;text-align:left;font-size:13px;min-height:44px}.history-footer .history-count{margin-left:auto;font-size:11px;color:#888}.history-footer:hover{color:#171717}.history-empty{padding:20px 10px;color:#888;font-size:12px;line-height:1.7}.history-menu{position:fixed;z-index:10010;width:188px;padding:5px;border:1px solid #e3e3e3;border-radius:9px;background:#fff;box-shadow:0 4px 14px #00000014;max-height:calc(100vh - 16px);overflow:auto}.history-menu button{width:100%;height:40px;display:flex;align-items:center;gap:12px;padding:0 12px;border:0;border-radius:5px;background:#fff;color:#343434;font-size:13px;text-align:left}.history-menu button:hover,.history-menu button:focus{background:#f3f3f4;outline:none}.history-rename{display:flex;flex:1;align-items:center;min-width:0;gap:2px;padding:8px 4px 8px 8px}.history-rename input{width:0;flex:1;min-width:0;padding:7px 6px;border:1px solid #777;border-radius:5px;background:white;font-size:13px}.history-error{color:#a33;font-size:11px;padding:0 10px 6px}.history-search{margin:0 8px 8px;padding:8px;width:calc(100% - 16px);border:1px solid #ddd;border-radius:6px;font-size:12px}.history-toast{position:fixed;z-index:10030;bottom:24px;left:50%;transform:translateX(-50%);display:flex;align-items:center;gap:18px;padding:12px 18px;background:#262626;color:white;border-radius:9px;font-size:13px;box-shadow:0 4px 16px #0002;max-width:calc(100vw - 32px)}.history-toast button{white-space:nowrap;color:#fff;text-decoration:underline;background:none;border:0;padding:3px}.history-share{width:min(520px,calc(100vw - 32px));max-height:calc(100vh - 40px);padding:24px;border:1px solid #ddd;border-radius:14px;color:#303030;box-shadow:0 12px 50px #0002}.history-share::backdrop{background:#0003}.history-share header{display:flex;align-items:center;justify-content:space-between}.history-share h2{font-size:18px;margin:0}.history-share h3{font-size:14px;overflow-wrap:anywhere}.history-share p{font-size:12px;color:#777;line-height:1.8}.history-share button{padding:8px 12px;border:1px solid #ddd;border-radius:7px;background:#fff;color:#333;font-size:13px}.history-share header button{border:0}.history-share .share-preview{max-height:240px;overflow:auto;border:1px solid #eee;border-radius:8px;padding:12px;font-size:13px;white-space:pre-wrap;overflow-wrap:anywhere}.history-share .share-preview p{color:#444}.history-share footer{display:flex;justify-content:flex-end;gap:8px;margin-top:18px;flex-wrap:wrap}.history-share .primary{background:#242424;color:white;border-color:#242424}.history-share input{width:100%;padding:9px;border:1px solid #ddd;border-radius:6px;font-size:12px}.history-share button:disabled{opacity:.5;cursor:wait}.history-share [role=alert]{color:#a33}.history-row button:focus-visible,.history-footer:focus-visible,.history-heading button:focus-visible,.history-share button:focus-visible{outline:2px solid #555;outline-offset:2px}.sidebar.collapsed .sidebar-history{display:none}@media(hover:none){.history-more{opacity:1}}`
}

/** Lives in the existing conversation scope, sharing its restore/save functions and state. */
function historyManagementBehavior(): string {
  return String.raw`
const historyUi={archived:false,query:'',editing:null,menu:null,anchor:null,collapsed:false};
const historyGlyphs=HISTORY_ICONS;
function historyToast(message,action,label='撤销'){
  document.querySelector('.history-toast')?.remove();const toast=document.createElement('div');toast.className='history-toast';toast.setAttribute('role','status');toast.append(document.createTextNode(message));
  if(action){const button=document.createElement('button');button.textContent=label;button.onclick=()=>{toast.remove();action()};toast.append(button)}document.body.append(toast);setTimeout(()=>toast.remove(),action?12000:5000);
}
function closeHistoryMenu(focus=false){const anchor=historyUi.anchor;document.querySelector('.history-menu')?.remove();document.querySelectorAll('.history-row.menu-open').forEach(row=>row.classList.remove('menu-open'));anchor?.setAttribute('aria-expanded','false');historyUi.menu=null;historyUi.anchor=null;if(focus&&anchor?.isConnected)anchor.focus()}
function ensureHistory(){let host=document.getElementById('history-list');if(!host){sidebar.insertAdjacentHTML('beforeend','<section class="sidebar-history" aria-label="历史对话"><div id="history-list" class="history-list"></div></section>');host=document.getElementById('history-list')}return host}
function historyTitle(item){return item.customTitle?item.title:item.title.replace(/^生成看板[：:]\s*/,'')||item.title}
function historyFocus(id){Array.from(document.querySelectorAll('.history-more')).find(button=>button.dataset.historyMore===id)?.focus()}
function historyUpdate(id,patch,message,undo){
  const item=state.history.find(item=>item.sessionId===id);if(!item)return false;const previous={...item};Object.assign(item,patch,{metadataUpdatedAt:Date.now(),updatedAt:Date.now()});
  try{saveHistory(state.history)}catch{state.history[state.history.indexOf(item)]=previous;historyToast('保存失败，请检查浏览器存储空间');return false}
  renderHistory();if(message)historyToast(message,undo);void window.flushWorkbenchHistory?.().catch(()=>historyToast('已保存在此浏览器，服务同步失败',()=>void window.flushWorkbenchHistory?.().then(()=>historyToast('已同步')).catch(()=>historyToast('同步失败，请稍后重试')),'重试'));return true;
}
function renderHistory(){
  const old=ensureHistory(),section=old.closest('.sidebar-history'),scroll=section.querySelector('.history-scroll')?.scrollTop||0;closeHistoryMenu();
  const archived=state.history.filter(item=>item.archivedAt),visible=state.history.filter(item=>historyUi.archived?item.archivedAt:!item.archivedAt).filter(item=>!historyUi.query||item.title.toLocaleLowerCase().includes(historyUi.query.toLocaleLowerCase())),pinned=visible.filter(item=>item.pinnedAt).sort((a,b)=>b.pinnedAt-a.pinnedAt),recent=visible.filter(item=>!item.pinnedAt).sort((a,b)=>(b.activityAt||b.createdAt)-(a.activityAt||a.createdAt));
  const row=item=>'<div class="history-row '+(item.pinnedAt&&!item.archivedAt?'is-pinned ':'')+(state.sessionId===item.sessionId?'is-active':'')+'" data-history-row="'+escapeHtml(item.sessionId)+'"><button class="history-item" type="button" data-session-id="'+escapeHtml(item.sessionId)+'" '+(state.sessionId===item.sessionId?'aria-current="true"':'')+' title="'+escapeHtml(item.title)+'&#10;'+escapeHtml(new Date(item.createdAt).toLocaleString('zh-CN',{year:'numeric',month:'numeric',day:'numeric',hour:'2-digit',minute:'2-digit'}))+'"><strong>'+escapeHtml(historyTitle(item))+'</strong></button><button class="history-more" type="button" data-history-more="'+escapeHtml(item.sessionId)+'" aria-label="更多操作：'+escapeHtml(item.title)+'" aria-haspopup="menu" aria-expanded="false">'+historyGlyphs.dots+'</button></div>';
  section.innerHTML='<div class="history-heading">'+(historyUi.archived?'<button type="button" data-history-back aria-label="返回历史对话">'+historyGlyphs['arrow-left']+'</button><span>已归档对话</span>':'<span>历史对话</span>')+'</div>'+(historyUi.archived?'<input class="history-search" type="search" placeholder="搜索已归档对话" aria-label="搜索已归档对话" value="'+escapeHtml(historyUi.query)+'">':'')+'<div class="history-scroll"><div id="history-list" class="history-list">'+(historyUi.archived?visible.sort((a,b)=>b.archivedAt-a.archivedAt).map(row).join(''):(pinned.length?'<div class="history-group"><button type="button" data-pin-collapse aria-expanded="'+(!historyUi.collapsed)+'">'+historyGlyphs.pin+'置顶 <span>'+pinned.length+'</span></button></div>'+(historyUi.collapsed?'':pinned.map(row).join('')):'')+recent.map(row).join(''))+(!visible.length?'<div class="history-empty">'+(historyUi.archived?(historyUi.query?'没有匹配的对话':'暂无已归档对话'):'暂无历史对话<br>开始分析后，对话会保存在这里')+'</div>':'')+'</div></div>'+(!historyUi.archived?'<button class="history-footer" type="button" data-history-archives>'+historyGlyphs.archive+'<span>已归档对话</span><span class="history-count">'+(archived.length||'')+'</span>'+historyGlyphs['chevron-right']+'</button>':'');
  section.querySelector('.history-scroll').scrollTop=scroll;
  section.querySelectorAll('.history-item').forEach(button=>button.onclick=()=>{if(state.sending&&state.sessionId!==button.dataset.sessionId){historyToast('请先停止当前回复，再切换对话');return}restoreHistory(button.dataset.sessionId)});
  section.querySelectorAll('.history-more').forEach(button=>button.onclick=()=>openHistoryMenu(button.dataset.historyMore,button));
  section.querySelector('[data-history-archives]')?.addEventListener('click',()=>{historyUi.archived=true;historyUi.query='';renderHistory();section.querySelector('.history-search')?.focus()});
  section.querySelector('[data-history-back]')?.addEventListener('click',()=>{historyUi.archived=false;historyUi.query='';renderHistory();section.querySelector('.history-footer')?.focus()});
  section.querySelector('[data-pin-collapse]')?.addEventListener('click',()=>{historyUi.collapsed=!historyUi.collapsed;renderHistory();section.querySelector('[data-pin-collapse]')?.focus()});
  section.querySelector('.history-search')?.addEventListener('input',event=>{const input=event.target,pos=input.selectionStart;historyUi.query=input.value;renderHistory();const next=section.querySelector('.history-search');next.focus();try{next.setSelectionRange(pos,pos)}catch{}});
}
function historyRename(id){
  const item=state.history.find(item=>item.sessionId===id),row=Array.from(document.querySelectorAll('[data-history-row]')).find(row=>row.dataset.historyRow===id);if(!item||!row)return;historyUi.editing=id;
  row.innerHTML='<form class="history-rename"><input aria-label="对话名称" maxlength="60" value="'+escapeHtml(item.title)+'"><button type="submit" aria-label="保存名称">'+historyGlyphs.check+'</button><button type="button" aria-label="取消重命名">'+historyGlyphs.x+'</button></form>';
  const input=row.querySelector('input');input.focus();input.select();const cancel=()=>{historyUi.editing=null;renderHistory();historyFocus(id)};row.querySelector('[aria-label="取消重命名"]').onclick=cancel;
  row.querySelector('form').onsubmit=event=>{event.preventDefault();const title=input.value.trim();if(!title){input.setCustomValidity('请输入对话名称');input.reportValidity();return}if(historyUpdate(id,{title,customTitle:true},'已重命名')){historyUi.editing=null;historyFocus(id)}};input.oninput=()=>input.setCustomValidity('');
  row.addEventListener('keydown',event=>{if(event.key==='Escape'){event.preventDefault();event.stopPropagation();cancel()}});
}
function openHistoryMenu(id,anchor){
  if(historyUi.menu===id){closeHistoryMenu(true);return}closeHistoryMenu();if(historyUi.editing){historyUi.editing=null;renderHistory();anchor=Array.from(document.querySelectorAll('.history-more')).find(button=>button.dataset.historyMore===id)}
  const item=state.history.find(item=>item.sessionId===id);if(!item||!anchor)return;historyUi.menu=id;historyUi.anchor=anchor;anchor.setAttribute('aria-expanded','true');anchor.closest('.history-row').classList.add('menu-open');
  const menu=document.createElement('div');menu.className='history-menu';menu.setAttribute('role','menu');menu.setAttribute('aria-label','对话操作');
  const actions=[['rename','pencil','重命名'],...(!item.archivedAt?[['pin','pin',item.pinnedAt?'取消置顶':'置顶']]:[]),['archive','archive',item.archivedAt?'恢复对话':'归档'],['share','share','分享']];
  menu.innerHTML=actions.map(([action,glyph,label])=>'<button type="button" role="menuitem" data-history-action="'+action+'">'+historyGlyphs[glyph]+label+'</button>').join('');document.body.append(menu);
  const rect=anchor.getBoundingClientRect(),height=menu.offsetHeight;menu.style.left=Math.max(8,Math.min(rect.right-6,innerWidth-menu.offsetWidth-8))+'px';menu.style.top=Math.max(8,rect.bottom+4+height<innerHeight?rect.bottom+4:rect.top-height-4)+'px';
  menu.querySelectorAll('button').forEach(button=>button.onclick=()=>{closeHistoryMenu();const action=button.dataset.historyAction;if(action==='rename'){historyRename(id);return}if(action==='pin'){historyUpdate(id,{pinnedAt:item.pinnedAt?0:Date.now()},item.pinnedAt?'已取消置顶':'已置顶');historyFocus(id)}else if(action==='archive'){const wasArchived=Boolean(item.archivedAt),previous={pinnedAt:item.pinnedAt||0,archivedAt:item.archivedAt||0};historyUpdate(id,{archivedAt:wasArchived?0:Date.now(),pinnedAt:0},wasArchived?'已恢复到历史对话':'已归档',()=>historyUpdate(id,previous,'已撤销'));document.querySelector('[data-history-back],.history-footer')?.focus()}else if(action==='share')openHistoryShare(item)});
  menu.querySelector('button').focus();menu.addEventListener('keydown',event=>{const buttons=Array.from(menu.querySelectorAll('button')),index=buttons.indexOf(document.activeElement);if(['ArrowDown','ArrowUp','Home','End'].includes(event.key)){event.preventDefault();buttons[event.key==='Home'?0:event.key==='End'?buttons.length-1:(index+(event.key==='ArrowDown'?1:-1)+buttons.length)%buttons.length].focus()}if(event.key==='Tab')closeHistoryMenu()});
}
function openHistoryShare(item){
  const snapshot=JSON.parse(JSON.stringify(item)),dialog=document.createElement('dialog');dialog.className='history-share';dialog.setAttribute('aria-labelledby','history-share-title');
  const turns=Array.isArray(snapshot.turns)&&snapshot.turns.length?snapshot.turns:[{stream:snapshot.stream}],messages=[];turns.forEach(turn=>{const stream=turn.stream||turn;if(stream?.question)messages.push({role:'用户',text:stream.question});if(stream?.output)messages.push({role:'通用数据智能体',text:stream.output})});
  dialog.innerHTML='<header><h2 id="history-share-title">分享对话</h2><button type="button" data-share-close aria-label="关闭分享">关闭</button></header><h3>'+escapeHtml(item.title)+'</h3><p>仅分享以下文字快照，不含附件、工具执行记录或看板访问权限。后续消息不会自动同步。</p><div class="share-preview">'+messages.map(message=>'<strong>'+escapeHtml(message.role)+'</strong><p>'+escapeHtml(message.text)+'</p>').join('')+'</div><p>本机链接仅限当前电脑访问。发给他人请下载对话副本，接收者可直接用浏览器打开。</p><div data-share-result></div><p role="alert" data-share-error></p><footer><button type="button" class="primary" data-share-create '+(!messages.length?'disabled':'')+'>生成分享快照</button></footer>';
  if(!messages.length)dialog.querySelector('[data-share-error]').textContent='这条对话暂无可分享的文字内容。';document.body.append(dialog);dialog.showModal();dialog.querySelector('[data-share-close]').onclick=()=>dialog.close();dialog.addEventListener('close',()=>{dialog.remove();historyFocus(item.sessionId)});dialog.addEventListener('click',event=>{if(event.target===dialog){const r=dialog.getBoundingClientRect();if(event.clientX<r.left||event.clientX>r.right||event.clientY<r.top||event.clientY>r.bottom)dialog.close()}});
  dialog.querySelector('[data-share-create]').onclick=async event=>{const button=event.target;button.disabled=true;button.textContent='正在生成…';try{const api=location.pathname.startsWith('/dsh-workbench')?'/api/dsh-workbench':'/api',response=await fetch(api+'/history/share',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({item:snapshot})});if(!response.ok)throw new Error('生成失败，请重试');const data=await response.json();if(!dialog.isConnected)return;const url=new URL(data.url,location.origin).href;
    dialog.querySelector('[data-share-result]').innerHTML='<label><p>本机只读链接</p><input aria-label="本机只读链接" readonly value="'+escapeHtml(url)+'"></label>';dialog.querySelector('footer').innerHTML='<button type="button" data-share-copy>复制本机链接</button><button type="button" class="primary" data-share-download>下载对话副本</button>';
    dialog.querySelector('[data-share-copy]').onclick=async()=>{try{await navigator.clipboard.writeText(url);dialog.querySelector('[data-share-copy]').textContent='已复制'}catch{dialog.querySelector('input').select();dialog.querySelector('[data-share-error]').textContent='自动复制未获允许，请复制已选中的链接。'}};
    dialog.querySelector('[data-share-download]').onclick=()=>{const blob=new Blob([data.html],{type:'text/html;charset=utf-8'}),href=URL.createObjectURL(blob),a=document.createElement('a');a.href=href;a.download=(snapshot.title.replace(/[\\/:*?"<>|]/g,'_').slice(0,60)||'对话')+'.html';a.click();setTimeout(()=>URL.revokeObjectURL(href),10000)};
    dialog.querySelector('[data-share-download]').focus();
  }catch(error){dialog.querySelector('[data-share-error]').textContent=error.message||'生成失败，请重试';button.disabled=false;button.textContent='重新生成'}};
}
document.addEventListener('click',event=>{if(historyUi.menu&&!event.target.closest('.history-menu,.history-more'))closeHistoryMenu()});
document.addEventListener('keydown',event=>{if(event.key==='Escape'&&(historyUi.menu||historyUi.editing||document.querySelector('.history-share'))){event.stopImmediatePropagation();event.preventDefault();if(document.querySelector('.history-share'))document.querySelector('.history-share').close();else if(historyUi.menu)closeHistoryMenu(true);else{const id=historyUi.editing;historyUi.editing=null;renderHistory();historyFocus(id)}}},true);
window.addEventListener('resize',()=>closeHistoryMenu());document.addEventListener('scroll',event=>{if(event.target?.classList?.contains('history-scroll'))closeHistoryMenu()},true);
window.addEventListener('workbench-history-loaded',event=>{state.history=event.detail;renderHistory()});
window.addEventListener('workbench-history-sync-failed',()=>historyToast('历史对话同步失败，已保留浏览器记录',()=>void window.flushWorkbenchHistory?.().then(()=>historyToast('已同步')).catch(()=>historyToast('同步失败，请稍后重试')),'重试'));
`.replace('HISTORY_ICONS', JSON.stringify(historyIcons))
}

export function applyHistoryManagement(page: string): string {
  return page.replace(/function ensureHistory\(\)\{[\s\S]*?function attachmentPopover\(\)/, historyManagementBehavior() + '\nfunction attachmentPopover()')
    .replace('</style>', historyManagementStyles() + '</style>')
}
