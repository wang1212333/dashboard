# 分享看板弹窗验收 — 2026-09-14（最新）

final result: passed

范围仅分享弹窗与原分享预检接口；下方保留前一项抽屉验收记录。

## 视觉目标与证据

- Source visual truth: `C:/Users/Administrator/AppData/Local/Temp/codex-clipboard-d5b18763-62cf-4167-a38c-6d8b9c442c9b.png`（1547×1017）。
- Implementation screenshots: `E:/DSH数据看板搭建项目/dsh-workbench/artifacts/share-modal/01-before.png`、`02-existing.png`、`03-form.png`、`04-success.png`、`05-failure.png`、`06-mobile.png`。
- 桌面 CSS viewport 1280×1000，截图工具按可见页面输出约 1280×981；初轮为 1280×720。窄屏 CSS viewport 375×812，截图约 375×795；抽屉实测宽 351.33px、左右安全边距约12px，无内容横向溢出。
- 比较在同一工具输入中同时打开参考双状态图与最终展开表单截图，并单独检查窄屏截图。参考为双弹窗展示板，实际为单弹窗；未冒充像素级同画布截图。按用户文字规范将参考约640px宽归一为实际520px，字体按17/14/13/12px实现，而非照搬放大展示图。
- 重点区域：标题/链接/人员标签/附言/底部操作均在全尺寸截图清楚可读，另用DOM测量44px链接框、36px按钮、纯白背景；无需额外放大裁片。

## 对照与迭代

初轮 P2：链接框48px超出40–44px要求；飞书区域标题只有13px正文层级。修复为44px框、14px/500标题，并重新截图对照。最终不存在未处理的P0/P1/P2视觉差异。

- 字体：继承中文无衬线体系，标题17/500，看板14/400，分区14/500，正文13/400，辅助12/400；不缩放页面字体。长名称两行省略，链接输入视觉省略但保留完整值。
- 间距与布局：520px、24px内边距、16px圆角；20–24px区间距；链接与可选发送上下分层，单容器85dvh内滚动。低视口高度允许滚动并保持操作可达，未强行固定所有状态高度。
- 颜色：#FFFFFF、#F6F7F8、#202124；14%遮罩，无渐变或蓝色主操作。只有原飞书消息卡片预览保留平台蓝色，不篡改发送效果。
- 图标/图像：官方Tabler close/copy/lock/check/chevron，统一尺寸与线宽，无自绘图片、装饰、头像。
- 文案：真实上下文绑定；本机权限说明与参考Jupyter示例不同是业务事实差异；复用标“可用”、新建标“已生成”；发送成功仅说明发送，不说明已读/授权。

## 交互与验证

见 `docs/share-modal-upgrade.md`。工作台/分享37项测试及分享服务7项测试通过；浏览器验证五个主要状态、预览、重试、同编号、防重复创建、管理返回、焦点限制与恢复、窄屏。真实工作台分享入口和服务端预检通过。

测试页统计轮询在重启时曾记录错误，已修复测试工具；新页面错误日志为空。生产分享弹窗无观察到的脚本错误。截图为隔离响应下的真实组件，不声明实际飞书送达；独立发送状态查询及明文live token恢复不存在，按能力边界处理。

## 实施检查表

- [x] 原分享入口、服务、消息格式复用
- [x] 白底轻量单弹窗、上下依赖流程
- [x] 真正预览不发送、错误/成功分色
- [x] 重试保留输入与请求编号
- [x] 桌面与375px、焦点/关闭/滚动验证
- [x] 生产入口读取与校验；不执行真实外部发送

---

# 图表说明抽屉验收 — 2026-09-14

final result: passed

范围：现有实时看板的 SQL inspector 与背景遮罩；不修改图表、业务查询、权限或筛选逻辑。

## 视觉对照

对照用户参考图与 artifacts/chart-description.png（1586×992）检查。采用需求指定的 600px 宽度，而不是复制参考截图约 706px 的像素宽度。白底、18px 左圆角、14% 黑色遮罩、轻阴影、32px 内容边距；标题 20/600、正文 14、辅助文字 13。页签浅灰，正文以留白和浅分隔线分组，仅来源和 SQL 有浅灰底。图标为官方 Tabler MIT 资源，无自绘装饰。真实条件有四行而非参考三行，因此正文高度不同，不伪造数据以贴合截图。

## 实测

- 现有 general-filter-acceptance/rev-0004 真实数据加载，国家排名和总量两种入口分别显示各自说明与 SQL；总量不显示排名限制。
- 图表说明、查询 SQL 页签正常；页签方法无请求调用，不修改筛选值。
- SQL 复制显示“已复制”；表名通过 Enter 复制显示“已复制”。复制源保留完整原文，显示格式单独处理。
- Esc 关闭后 focus 回到 ranking 入口；页面 scrollY 关闭前后均为 1076.6666，overflow 从 hidden 恢复原值。
- Shift+Tab 从关闭按钮循环到最后一个可交互入口。
- 390×844：抽屉左边界 0，宽390；正文无横向溢出，长表名换行。SQL 区342px、内容658px，只代码区横向滚动。头部页签不参与正文滚动。
- 构建通过；27 项 inspector/live-dashboard/live-preview-bridge 测试通过（testTimeout=15000）。首次默认5秒阈值有一项本地分享服务测试超时，扩展阈值后两轮通过。
- 最终3080部署HTTP200，已确认包含初始化调用及保留SQL换行的最新脚本。

## 诚实边界

- 固定条件 versions、whether_apply_adjustment 缺少中文元数据名称，显示“字段名称暂未提供”，值仍为真实执行条件。没有从值或SQL猜业务含义。
- 当前数据模型只支持单个来源表；不构造不存在的多表来源。
- SQL为提交给网关的实际执行请求，无法显示网关内部权限重写后的语句，界面已标注。
- 没有在真实用户页面注入复制失败、缺SQL、缺来源等故障；这些分支已实现但未做浏览器故障注入验证。未验证所有历史自定义图表/浏览器组合。
- reduced-motion、空状态经代码核对；未以操作系统设置实测减少动态效果。

## 文件

- src/live-dashboard/inspector.ts：替换原有抽屉内部视图与交互，保留来源入口与事件数据。
- src/live-dashboard/inspector-icons.ts、TABLER-LICENSE.txt：官方图标及许可。
- src/live-dashboard/service.ts：追加字段说明与SQL执行标识，不更改查询。
- src/live-dashboard/preview-bridge.ts：旧版本页面仅替换已标记的 inspector 脚本，不覆写保存的看板。
- tests/inspector.spec.ts：初始化执行和旧版本脚本替换回归。

使用 image-to-code 技能的视觉比较与真实页面验收流程，未增加新UI框架。
