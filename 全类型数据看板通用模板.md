# 全类型数据看板通用模板

> 适用范围：经营分析、监控预警、项目管理、客户分析、财务分析、市场研究、产品分析、数据探索、公共信息展示等所有数据看板。
>
> 目标：让使用者在最短路径内理解“数据在说什么、为什么重要、能否相信、如何进一步探索”。

---

## 1. 先选择看板类型，而不是先选择图表

| 看板类型 | 用户的核心问题 | 推荐重点 |
|---|---|---|
| 状态总览 | 现在处于什么状态？ | KPI、当前值、趋势、状态标识 |
| 监控预警 | 是否发生了需要关注的变化？ | 阈值、异常、时间序列、通知与处理记录 |
| 分析诊断 | 为什么变化/差异会发生？ | 分解、对比、下钻、贡献度、明细 |
| 规划决策 | 在约束下应该选择什么方案？ | 情景模拟、优先级、约束、预测、推荐 |
| 探索研究 | 数据中有什么规律？ | 灵活筛选、维度切换、分布、关联、明细导出 |
| 进度协同 | 工作推进到哪里、卡在哪里？ | 里程碑、责任、状态、依赖、待办 |
| 对外展示 | 希望受众理解什么事实或故事？ | 叙事顺序、简明结论、说明文字、有限交互 |

一个看板可以有多个目的，但必须确定一个主目的；首页优先服务主目的，其余目的放在二级页或下钻层。

---

## 2. 通用信息架构

```text
01 语境：这份看板在回答什么问题？数据截至何时？
02 结论：最重要的 3–5 个事实或指标是什么？
03 变化：与目标、历史或对照组相比发生了什么？
04 结构：变化集中在哪些维度或对象？
05 解释：驱动因素、关联因素或规则说明是什么？
06 探索：用户如何筛选、切换、下钻和查看明细？
07 可信度：数据从哪里来、口径为何、是否完整？
```

不是每个页面都要包含所有层，但任何看板都应让用户能够从结论走到证据。

---

## 3. 首页标准版式

### A. 标题与语境区

`看板名称｜一句话目的｜数据范围｜数据截止时间｜刷新状态｜口径/帮助入口`

示例：`客户服务质量总览｜监控服务效率与体验变化｜中国区全渠道｜截至 2026-08-31｜每日更新`

### B. 全局筛选区

按需选择：`时间｜组织/区域｜业务线｜对象类型｜渠道｜状态｜对比基准`。

规则：筛选项只保留会明显改变结论的维度；筛选后的影响范围需清晰可见；支持一键复位。

### C. 核心结论区

3–6 个 KPI 或结论卡，统一展示：

`指标名称｜当前值｜对比值｜变化量/变化率｜目标/阈值｜趋势方向｜简短解释`

### D. 主叙事区

用 1–3 个图表回答主问题。常用结构：

- **现状 + 趋势**：当前指标卡 + 时间序列；
- **规模 + 结构**：总量卡 + 分组/占比图；
- **结果 + 驱动**：结果趋势 + 贡献分解/散点/漏斗；
- **计划 + 进度**：目标/里程碑 + 实际完成与剩余。

### E. 深入探索区

排名、矩阵、地图、分布、明细表或下钻入口。将复杂细节放在这里，避免首页变成数据墙。

### F. 数据可信区

`数据来源｜更新时间｜覆盖范围｜缺失说明｜计算口径｜联系人/责任人`。

---

## 4. 指标卡与对比模板

每张指标卡至少包含：

| 字段 | 说明 |
|---|---|
| 指标名称 | 面向业务语言，不直接暴露字段名 |
| 当前值 | 当前筛选范围内的结果 |
| 对比基准 | 目标、上期、去年同期、预算、行业基准或对照组 |
| 变化 | 绝对变化与相对变化，避免只显示一种 |
| 状态 | 正常/关注/异常；状态颜色必须有文字/图标补充 |
| 解释 | 一句说明变化含义或主要驱动 |
| 口径 | 可打开查看定义、公式、粒度与数据源 |

对比必须回答：与什么比、比的是哪个周期、分母是什么、是否同口径。

---

## 5. 图表选择模板

| 要表达的关系 | 首选图表 | 避免 |
|---|---|---|
| 时间变化 | 折线图、面积图、柱线组合图 | 多于 6–8 条无重点的折线 |
| 分类比较 | 条形图、排序表 | 未排序的柱形图、3D 图 |
| 构成/占比 | 堆叠条形图、100% 堆叠、矩形树图 | 类别过多的饼图 |
| 目标进展 | 子弹图、进度条、目标线 | 只写百分比不写目标量 |
| 分布/离散 | 直方图、箱线图、散点图 | 用均值掩盖离散度 |
| 相关/权衡 | 散点图、气泡图、四象限 | 把相关性直接解释为因果 |
| 路径/流向 | 漏斗图、桑基图、流程时间轴 | 不带数量的装饰性流程图 |
| 空间差异 | 地图、分级色块、区域排名 | 地理含义不重要时强行使用地图 |
| 多指标对象比较 | 矩阵、热力表、可排序明细表 | 雷达图堆叠过多对象 |

基本准则：一张图只表达一个核心关系；颜色用于分类或状态，不用于装饰；所有图表都应有单位、时间范围和必要的基准线。

---

## 6. 交互与下钻模板

### 筛选

- 全局筛选：影响整页，适合时间、组织、区域等共性维度。
- 局部筛选：只影响一个分析模块，适合避免误解或保留对照。
- 联动筛选：点击图表对象即可过滤其它模块；应提供清晰的已选状态和复位入口。

### 筛选控件必须可用

“筛选区”不能只是展示字段名称。只要页面声明了可筛选维度，就必须渲染真实控件，并满足以下规则：

| 要素 | 最低要求 |
|---|---|
| 控件 | 下拉、多选、日期/周期选择或搜索框；选项来自实际数据，不写死示例值 |
| 默认态 | 打开页面即可看到有价值的全量或默认范围结论，不能要求用户先筛选 |
| 联动范围 | 全局筛选同步刷新 KPI、图表、结论/预警、明细和当前记录数；局部筛选必须标明影响范围 |
| 已选状态 | 显示已选条件，支持逐项移除与“一键重置” |
| 结果反馈 | 明确显示“当前筛选命中 X / Y 条记录”、时间范围或无结果提示 |
| 依赖关系 | 上级维度改变时，收窄下级选项；例如地区部 → 国家 → 客户/产品 |
| 大量选项 | 超过 50 个选项时提供搜索；超过 200 个时默认不展开全部明细 |
| 离线预览 | 可使用嵌入的受控数据快照在浏览器本地联动；不得依赖未声明的网络请求 |

每个筛选条件的计算必须基于完整的可用数据集，而不能只筛选当前页面已经展示的前 20 或 30 行明细。

### 下钻

设计为固定层级，而不是让用户猜下一步：

```text
总览 → 分类/区域/人群 → 对象 → 明细记录
```

每层都要保留上一层筛选条件与返回路径。下钻目的应是解释结论，而不是展示更多字段。

### 明细

推荐字段：`对象｜当前值｜对比值｜变化｜贡献/占比｜状态｜更新时间`；支持排序、搜索、导出（如需要）和查看口径。

---

## 7. 视觉与组件合同

信息架构解决“放什么”，视觉合同解决“怎样稳定地呈现”。每个可生成看板应同时携带一份可复用的视觉与组件配置，而不是由生成器临时猜测配色和圆角。

### 7.1 最小视觉合同

| 类别 | 必须配置 | 说明 |
|---|---|---|
| 画布 | `canvas`、`surface`、`elevated` | 页面背景、基础卡片、强调/诊断区域 |
| 色彩语义 | `primary`、`success`、`warning`、`danger`、`text`、`muted`、`hairline` | 颜色必须有业务含义；状态不能只靠颜色表达 |
| 字体 | 标题、正文、数字/等宽字体 | 统一字号阶梯与数字对齐方式 |
| 布局 | 最大内容宽度、栅格列数、模块间距、卡片/控件圆角 | 桌面端与小屏端分别定义 |
| 图表 | 主色序列、网格线、基准线、图例、Tooltip、空态 | 同一指标在不同模块中保持同一色彩语义 |
| 组件状态 | 默认、悬停、选中、禁用、加载、空数据、错误 | 筛选器、卡片、标签、表格和图表都需覆盖 |

推荐将其保存为 `visualContract`：

```text
风格名 / 版本
色板 token
字体 token
间距、栅格与圆角 token
KPI 卡、筛选器、图表容器、状态标签、明细表的组件规则
桌面 / 平板 / 手机断点规则
```

默认可使用“中性浅色业务看板”风格：浅灰画布、白色卡片、单一主色、12px 卡片圆角；但它只是保底，不应替代已经确认的品牌或黄金案例风格。

### 7.2 标准组件清单

| 组件 | 必含内容 | 行为要求 |
|---|---|---|
| 标题栏 | 名称、目的、数据范围、截止时间、刷新状态、口径入口 | 截止时间和刷新状态始终可见 |
| 筛选器 | 标签、当前值、选项、重置 | 全局联动，支持键盘操作与无结果提示 |
| KPI 卡 | 名称、当前值、对比、状态/趋势、简短解释 | 一个卡只承载一个主指标；点击可进入对应分析或明细 |
| 图表容器 | 标题、问题、单位、图例/基准、空态 | 图表与其问题一一对应，支持 Tooltip 或直接标签 |
| 状态/预警标签 | 状态名称、等级、阈值或原因 | 不能只用红黄绿；必须有文字或图标 |
| 行动卡 | 优先级、依据、下一步、负责人/截止时间（如适用） | 操盘类看板应可进入处理或下钻 |
| 明细表 | 对象、关键指标、对比、状态、更新时间 | 默认排序明确，支持搜索、筛选、下钻或导出（按需） |
| 可信度区 | 来源、口径、刷新、覆盖范围、限制 | 任何数据异常或语义未验证均需显式提示 |

### 7.3 响应式规则

- 桌面端：KPI 采用 3–6 列自适应栅格；主叙事图与结构图可双列。
- 平板端：筛选器换行，KPI 缩为 2–3 列，图表改为单列或 6:4 布局。
- 手机端：标题、核心结论、筛选和首张主图优先；KPI 单列/双列，表格以横向滚动或卡片明细呈现。
- 不允许因小屏而隐藏数据截止时间、已选筛选条件、核心状态或数据限制。

## 8. 数据可信度模板

任何看板都至少说明：

`数据来源｜刷新频率｜最后成功刷新时间｜数据覆盖范围｜计算口径｜缺失/延迟说明｜数据责任人`

关键指标应有指标字典：

`名称｜定义｜公式｜统计周期｜粒度｜维度｜来源字段｜过滤条件｜负责人`

并对以下情况显式提示：数据延迟、缺失、抽样、估算、口径变更、异常值剔除。

---

## 9. 通用线框模板

```text
┌──────────────────────────────────────────────────────────────────┐
│ 看板名称 ｜ 一句话目的 ｜ 数据截至日期 ｜ 刷新状态 ｜ 帮助/口径      │
├──────────────────────────────────────────────────────────────────┤
│ 时间  [____]  组织/区域 [____]  对象 [____]  状态 [____]  重置     │
├───────────────┬───────────────┬───────────────┬──────────────────┤
│ 核心指标 1    │ 核心指标 2    │ 核心指标 3    │ 核心指标 4       │
│ 当前/对比/解释│ 当前/对比/解释│ 当前/对比/解释│ 当前/对比/解释   │
├──────────────────────────────────┬───────────────────────────────┤
│ 主图：变化/趋势/进度              │ 结构图：分组、构成或驱动      │
├──────────────────────────────────┴───────────────────────────────┤
│ 结论与异常/机会清单：对象｜变化｜原因｜下一步/下钻入口             │
├──────────────────────────────────────────────────────────────────┤
│ 深入探索：排名、矩阵、地图、分布或可排序明细表                     │
├──────────────────────────────────────────────────────────────────┤
│ 数据来源、口径、更新时间、覆盖范围、已知限制                       │
└──────────────────────────────────────────────────────────────────┘
```

---

## 10. 上线前验收清单

- 看板标题能在一句话内说明用户、问题和数据范围。
- 首页 30 秒内可看出当前状态与最重要变化。
- 每个 KPI 有明确的对比基准、单位、时间范围和口径。
- 图表选择与要表达的关系匹配，未用装饰性图表替代分析。
- 用户可从结论下钻到原因和明细，路径不超过 3 步。
- 过滤与联动范围明确，复位容易，默认视图有价值。
- 页面中声明的筛选条件均有真实控件，并会联动刷新 KPI、图表、行动区和明细；筛选计算不依赖截断后的展示行。
- 视觉合同已配置：色彩、字体、间距、圆角、图表状态和小屏规则均可复用；未确认品牌风格时才使用默认风格。
- 数据来源、刷新时间、覆盖范围和已知限制可见。
- 异常/预警类看板能进入处理闭环；展示/研究类看板能导出或分享证据。
- 移动端或小屏使用时，核心结论和筛选仍然可读可用。

---

## 11. 建设时的最小输入表

在开始设计前先填写以下表格：

| 项目 | 内容 |
|---|---|
| 目标用户 | 谁每天/每周会使用？ |
| 使用场景 | 例会、实时监控、专题分析、对外汇报还是自助探索？ |
| 主问题 | 使用者最终要判断或完成什么？ |
| 主指标 | 最多 3–5 个，及各自口径与基准 |
| 关键维度 | 时间、组织、区域、对象、渠道等 |
| 决策/动作 | 看完后可能采取什么行动？ |
| 数据来源 | 数据表、负责人、刷新频率、质量限制 |
| 视觉偏好 | 品牌/黄金案例、深浅色、信息密度、字体与可访问性要求 |
| 交付边界 | 是否需要下钻、明细、导出、订阅、权限、移动端？ |

当这张表无法填写时，应先澄清业务问题，不急于画图或搭建页面。
